{
  "name": "WordApp",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "babel-core": "^6.4.1",
    "babel-loader": "^6.4.1",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-react": "^6.23.0",
    "bootstrap": "4.0.0-alpha.6",
    "socket.io-client": "1.7.3",
    "tether": "1.4.0",
    "webpack": "^2.0.0"
  },
  "scripts": {
    "watch": "NODE_ENV=development webpack -w --env=dev --progress --profile --colors",
    "build": "NODE_ENV=production  webpack -p --env=prod --progress --profile --colors"
  }
}
